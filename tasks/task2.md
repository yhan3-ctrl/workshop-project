# Settings utility

A teammate uses `task2/config_writer.py` to update an app's settings, but the
changes don't seem to be saved to `settings.json`. Please use the agent to fix
`update_settings` so it saves the new theme and font size, without changing the
other settings. Let me know when you think it's done.
